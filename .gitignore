# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# 個人工作日誌（含內部專案痕跡）→ 保留在本機、不入庫
process.log

# examples/ 的 YAML 是官方可執行範例，納入版控；衍生產物仍排除。

# render 產物（皆由 .wf.yaml 經 render.sh / wfyaml.py 衍生，可重生 → 不入庫）
*.html
*.png
*.svg
*.dot
*.pdf
*.debug.html
prototype*.html

# 注意：assets/*.json.gz（圖庫）與 assets/wf.css（封印 CSS）是自帶資產、runtime 必需 → 照常入庫
# style 素材（border 筆觸圖等）也是 runtime 必需 → 覆寫上面的 *.png/*.svg 排除
!assets/styles/**
